/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'localhost',
      '10.0.0.187',
      'ik.imagekit.io',
    ],
  },
}

module.exports = nextConfig
